[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "json-schema-validator"
version = "0.1.0"
description = "Validates JSON documents against a JSON Schema."
